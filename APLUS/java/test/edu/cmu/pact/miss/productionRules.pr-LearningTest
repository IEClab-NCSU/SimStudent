;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Jess Production Rules for a Cognitive Tutor
;;
;; This file was automatically generated by a Simulated Student.
;; Date of creation: February 22, 2007 03:39:40 PM
;;
;; PSLC/CTAT Simulated Student Project, 2005, 2006
;; Carnegie Mellon University
;;

;; Removing a following line will jeopardize the tutor's model tracing
;; capability
;; 
;; You may need to add similar statements if you wish to add your own
;; predicates and operator symbols.  See
;; http://herzberg.ca.sandia.gov/jess/docs/70/extending.html for details
;; 
(load-package SimStEquation.UserDefSymbols)

;; ----------------
;; Production rules
;; ----------------

(defrule div-lhs

?var0 <- (problem  (interface-elements ? ? ?var11 ?)    )
?var11 <- (table  (columns ?var12))
?var12 <- (column  (cells ?var13 ? ? ? ? ? ? ?)  )
?var13 <- (cell  (value ?val0&~nil)   )

?var0 <- (problem  (interface-elements ? ?var1 ? ?)    )
?var1 <- (table  (columns ?var2))
?var2 <- (column  (cells ?var3 ? ? ? ? ? ? ?)  )
?var3 <- (cell  (value ?val1&~nil)   )

?var0 <- (problem  (interface-elements ? ? ?var11 ?)    )
?var11 <- (table  (columns ?var12))
?var12 <- (column  (cells ? ?var14 ? ? ? ? ? ?)  )
?var14 <- (cell  (name ?selection) (value ?input&nil)   )

(test (distinctive ?var13 ?var14))
(test (same-column ?var13 ?var14))
(test (consecutive-row ?var13 ?var14))
(test (same-table ?var13 ?var14))
(test (distinctive ?var3 ?var14))
(test (same-column ?var3 ?var14))
(test (consecutive-row ?var3 ?var14))

(test (has-const-term ?val1 ) )

=>

(bind ?input (rip-coefficient ?val0))
(predict-algebra-input ?selection UpdateTable ?input )
(modify ?var14 (value ?*sInput*))

)


