/**
*
*/
function getSafeElementById(anID) 
{
	/*
    var elem = app.getElementById(anID);
  
    var bExists = elem != null && Object.keys(elem).length < 100;
  
    return bExists ? elem : null;
	*/
	
	return (new Object ());
}